# May 2026

| Date | Entry |
|------|-------|
| 08 | [Sandbox Bridge Host-Owned Metered Services](08-Sandbox-Bridge-Host-Owned-Metered-Services.md) — archived the completed host-owned sandbox bridge slice: canonical `/bridge/metered-services/ws`, no legacy `/comms/...` route mounting, `pkg/sandbox/comms/*` moved under `pkg/sandbox/bridge/*`, guest-local metered-services forwarding over a host-opened WebSocket, host-stamped sandbox identity, `pkg/x402` backend reuse, OpenClaw and Hermes helper/context wiring, two-process bridge integration coverage, direct host callback/origin regression coverage, and explicit separation from chat and Skylink; live sandbox/Hermes smoke, real-USDC guards, status persistence, handshake hardening, and package rename decisions moved to `docs/work/todo/sandbox-bridge-followup.md` |
| 06 | [x402 Market Services Integration](06-X402-Market-Services-Integration.md) — archived the integrate-market-services slice: `pkg/x402` protocol core with v1/v2 dual-version dispatch and PreferAndCheapest selection, OWS-backed EIP-3009 (Base) and v0 versioned (Solana) signing paths with `BalanceMicros` preflight, three retry-header compatibility (`X-Payment`/`Payment-Signature`/`X-402-Payment`), hybrid v2 envelope with verbatim `accepted` round-trip, version-blind receipt parsing including bare-hex fallback, agentic.market discovery + curated overlay + hourly refresh, host-side `x402.*` JSON-RPC, sandbox-comms endpoint at `/comms/x402/ws`, Web Settings → Services with per-service approval, budget card, receipts log, search/filter, and wallet-status banner, `pkg/x402/siwx` Sign-In-With-X integration with EIP-191 personal_sign and `X-Sign-In-With-X` header, live verification across Exa, Blockrun, Smartflow, Browserbase, Alchemy, Coingecko, Messari, and a full Venice top-up + chat-completions + balance flow, captured wire fixtures under `pkg/x402/testdata/`; M7 telemetry, M8 quality/reputation, and M10 OpenClaw plugin remain active in `docs/work/current/x402/` |
