# 2026

| Month | Summary |
|-------|---------|
| [May](05/) | x402 market services integration: `pkg/x402` protocol core with v1/v2 dispatch and PreferAndCheapest, OWS-backed EIP-3009 (Base) and v0 versioned (Solana) signing with wallet preflight, three retry-header compatibility, hybrid v2 envelope, version-blind receipt parsing, agentic.market discovery + overlay + hourly refresh, Web Settings → Services with approval/budget/receipts UI, Sign-In-With-X integration, live-verified across Exa/Blockrun/Smartflow/Browserbase/Alchemy/Coingecko/Messari plus a full Venice top-up + chat-completions + balance flow; host-owned sandbox bridge for metered services with canonical `/bridge/metered-services/ws`, no legacy `/comms/...` mounting, guest-local forwarding over a host-opened WebSocket, host-stamped identity, OpenClaw/Hermes helper wiring, and follow-up smoke/hardening tracked in todo |
| [April](04/) | Identity layer, private-network discovery hardening, invite/bootstrap hardening, KV causal sync hardening, multi-instance e2e foundation, device-subsystem foundation, agent support, OpenClaw and Hermes Lima sandboxes, Lima + Docker guest runtimes, Lima VM hardening with explicit forwarded endpoints and direct sandbox-agent listing, Lima hardening follow-up with Hermes config cleanup, `sandbox.runtime.*`, `system.health`, and runtime drift UI, OpenClaw/Hermes runtime bundle relocation into `external/runtimebundles`, host-initiated guest WebSocket comms groundwork, Hermes websocket streaming + host web chat cutover, OpenClaw websocket streaming follow-up, websocket file/media attachments for Hermes and OpenClaw, ChatGPT Codex OAuth + brokered chat, guest-runtime rollout and self-upgrade gotchas, AI-first prompt-to-agent creation with versioned `AgentSpec`, spec fixtures, RPC tool wrappers, sandbox secret binding, job/artifact groundwork, and a fresh-VM OpenClaw Docker smoke, brokered messaging adapters with IMAP/SMTP, Slack, JSON-RPC stdio adapter protocol, policy/approval boundary, shim surface, and settings UI, secrets, mailbox, private-network robustness, live relay productization, transport-vs-coordination clarity, P2P CPU/public-network load mitigation with unvalidated P2P performance concerns, system-aware web dark theme + selector, Syncthing-informed FS P2P hardening, Windows path-policy work, Windows CLI/installer/tray readiness with the Windows agent hypervisor still unimplemented, S3-optional P2P, self-update, staged updater/tray recovery, agent wallet & payments, Base wallet support, managed helper apps |
| [March](03/) | SkyFS V1-V3: encrypted file storage → multi-device sync → continuous sync daemon, web command center, 124 tests |
