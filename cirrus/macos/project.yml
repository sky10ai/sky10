name: Cirrus
options:
  bundleIdPrefix: ai.sky10
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"

targets:
  Cirrus:
    type: application
    platform: macOS
    sources:
      - path: cirrus
        excludes:
          - Resources
      - path: cirrus/Resources/Assets.xcassets
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ai.sky10.cirrus
        PRODUCT_NAME: Cirrus
        MARKETING_VERSION: "0.13.10"
        CURRENT_PROJECT_VERSION: "1"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        SWIFT_VERSION: "5.10"
        PRODUCT_MODULE_NAME: cirrus
        INFOPLIST_KEY_LSUIElement: true
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    info:
      path: cirrus/Info.plist
      properties:
        CFBundleName: Cirrus
        CFBundleDisplayName: Cirrus
        CFBundleIdentifier: ai.sky10.cirrus
        CFBundleVersion: "1"
        CFBundleShortVersionString: "0.13.10"
        CFBundlePackageType: APPL
        CFBundleExecutable: Cirrus
        LSMinimumSystemVersion: "14.0"
        LSUIElement: true
        CFBundleIconFile: Cirrus

  Cirrus-Tests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: cirrus-tests
    dependencies:
      - target: Cirrus
    settings:
      base:
        SWIFT_VERSION: "5.10"
